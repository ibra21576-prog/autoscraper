services:
  - type: web
    name: autoscraper
    runtime: docker
    plan: free
    envVars:
      - key: LIVE_SCRAPE_ENABLED
        value: "false"
      - key: SECRET_KEY
        generateValue: true
